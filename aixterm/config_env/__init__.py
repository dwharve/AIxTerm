"""Configuration package for AIxTerm."""

# Note: AIxTermConfig is imported from the parent config module to maintain compatibility
# but we avoid importing it here to prevent circular dependencies during initialization
# Import env_vars directly when needed

__all__ = ["env_vars"]